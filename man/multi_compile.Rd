% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_compile.R
\name{multi_compile}
\alias{multi_compile}
\alias{multi_compile.MultiObjConservationProblem}
\alias{multi_compile.list}
\title{Compile a multi-objective optimization problem}
\usage{
multi_compile(x, ...)

\method{multi_compile}{MultiObjConservationProblem}(x, ...)

\method{multi_compile}{list}(x, ...)
}
\arguments{
\item{x}{\code{\link[=multi_problem]{multi_problem()}} or \code{list} of \code{\link[=OptimizationProblem-class]{OptimizationProblem}}
objects.}

\item{...}{arguments passed to \code{\link[=compile]{compile()}}.}
}
\value{
A \code{list} containing a (\verb{$obj}) \code{numeric} matrix with the coefficients
for each of the objectives (i.e., rows correspond to different
objectives and columns correspond to different decision variables),
(\verb{$modelsense}) \code{character} vector indicating if each
objective should be maximized or minimized
(i.e., each element corresponds to a different objective),
and a (\verb{$opt}) \code{\link[=OptimizationProblem-class]{OptimizationProblem}} object with all
of the constraints present in \code{x} (note that the objective coefficients
in the returned object are all zero).
}
\description{
Compile multiple \code{\link[=OptimizationProblem-class]{OptimizationProblem}} objects for
multi-objective optimization.
}
\examples{
\dontshow{if (prioritizr::do_run_example()) withAutoprint(\{ # examplesIf}
# import data
sim_pu_raster <- get_sim_pu_raster()
sim_features <- get_sim_features()

# define a total conservation budget (30\% of total cost)
budget <- terra::global(sim_pu_raster, "sum", na.rm = TRUE)[[1]] * 0.3

# create multi-objective conservation planning problem
mp <-
  multi_problem(
    keystone_obj =
      problem(sim_pu_raster, sim_features[[1:3]]) \%>\%
      add_min_shortfall_objective(budget) \%>\%
      add_relative_targets(0.4) \%>\%
      add_binary_decisions(),
    iconic_obj =
      problem(sim_pu_raster, sim_features[[4:5]]) \%>\%
      add_min_shortfall_objective(budget) \%>\%
      add_relative_targets(0.45) \%>\%
      add_binary_decisions()
  )

# compile into multi-objective optimization problem
mo <- multi_compile(mp)

# print multi-objective optimization problem
print(mo)
\dontshow{\}) # examplesIf}
}
\seealso{
See \code{\link[=compile]{compile()}} to create an \code{\link[=OptimizationProblem-class]{OptimizationProblem}} object.
}
