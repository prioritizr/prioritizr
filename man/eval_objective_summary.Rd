% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval_objective_summary.R
\name{eval_objective_summary}
\alias{eval_objective_summary}
\alias{eval_objective_summary.ConservationProblem}
\alias{eval_objective_summary.MultiObjConservationProblem}
\title{Evaluate objective value of solution}
\usage{
eval_objective_summary(x, solution, include_penalties = TRUE)

\method{eval_objective_summary}{ConservationProblem}(x, solution, include_penalties = TRUE)

\method{eval_objective_summary}{MultiObjConservationProblem}(x, solution, include_penalties = TRUE)
}
\arguments{
\item{x}{\code{\link[=problem]{problem()}} or \code{\link[=multi_problem]{multi_problem()}} object.}

\item{solution}{\code{numeric}, \code{matrix}, \code{data.frame},
\code{\link[terra:rast]{terra::rast()}}, or \code{\link[sf:sf]{sf::sf()}} object.
The argument should be in the same format as the planning unit cost
data in the argument to \code{x}.
See the Solution format section for more information.}

\item{include_penalties}{\code{logical} should penalties be included when
calculating objectives values? Defaults to \code{TRUE}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} object containing the solution cost.
It contains the following columns:

\describe{

\item{problem}{\code{character} name of problem. Note that this column
is only present if \code{x} is a \code{\link[=multi_problem]{multi_problem()}} object.}

\item{value}{\code{numeric} objective value.}

}
}
\description{
Calculate the objective value of a solution to a conservation planning
problem.
}
\details{
The mathematical objective function of an optimization problem describes
the performance metric that is minimized or maximized during
optimization.
In a conservation planning \code{\link[=problem]{problem()}}, \link{objectives} specify the primary
metric should be maximized or minimized (e.g., \code{\link[=add_min_set_objective]{add_min_set_objective()}}
specify that costs should be minimized) and \link{penalties} can
(optionally) be used to specify additional metrics that should be maximized
or minimized during optimization
(e.g., \code{\link[=add_boundary_penalties]{add_boundary_penalties()}} specify that spatial
fragmentation should be minimized).
Given this, the mathematical objective function of a
conservation planning \code{\link[=problem]{problem()}} is calculated based on
a weighted sum of the \link{objectives} and \link{penalties}
(i.e., where the weights are the \code{penalty} values specified
in the \link{penalties} function).
}
\examples{
# TODO

}
\seealso{
Other functions for summarizing solutions:
\code{\link{eval_asym_connectivity_summary}()},
\code{\link{eval_boundary_summary}()},
\code{\link{eval_connectivity_summary}()},
\code{\link{eval_cost_summary}()},
\code{\link{eval_feature_representation_summary}()},
\code{\link{eval_n_summary}()},
\code{\link{eval_target_coverage_summary}()}
}
\concept{summaries}
